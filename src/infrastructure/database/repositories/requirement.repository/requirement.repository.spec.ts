import { RequirementRepository } from './requirement.repository';

describe('RequirementRepository', () => {
  it('should be defined', () => {
    expect(new RequirementRepository()).toBeDefined();
  });
});
