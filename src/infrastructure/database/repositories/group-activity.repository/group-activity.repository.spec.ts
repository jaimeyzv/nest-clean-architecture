import { GroupActivityRepository } from './group-activity.repository';

describe('GroupActivityRepository', () => {
  it('should be defined', () => {
    expect(new GroupActivityRepository()).toBeDefined();
  });
});
