import { GroupActivityEntity } from './group-activity.entity';

describe('GroupActivityEntity', () => {
  it('should be defined', () => {
    expect(new GroupActivityEntity()).toBeDefined();
  });
});
