import { CreateGroupActivityUsecase } from './create-group-activity.usecase';

describe('CreateGroupActivityUsecase', () => {
  it('should be defined', () => {
    expect(new CreateGroupActivityUsecase()).toBeDefined();
  });
});
